<script>
  let availableTables = JSON.parse('<%- JSON.stringify(tables) %>'); // initially empty, fetched from server by ajax
  // to set checked in edit mode:
  const reservedTables = JSON.parse('<%- JSON.stringify(data?.tables || []) %>');
</script>
<script defer src="/javascripts/reservations.js"></script>
<h2 class="page-header">Edit Reservation</h2>
<form action="/reservations/<%= data._id %>/?_method=PUT" method="POST">
  <%- include('_form-fields') %>
  <%- include('_extra-form-fields') %>

  <div class="form-row form-row-end btn-row">
    <a class="btn btn-danger" href="/reservations">Cancel</a>
    <button class="btn btn-primary" type="submit">Update</button>
  </div>
</form>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    fetchAvailableTables(); 
  })
</script>