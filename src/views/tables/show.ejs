<h2 class="page-header"><%= book.title %></h2>
<div class="table-details">
  <div>
    <img class="table-cover" src="<%= book.coverImagePath %>">
    <div class="table-details-btn-grid">
      <a class="btn btn-primary" href="/books/<%= book.id %>/edit">Edit</a>
      <%- include('../partials/deleteForm', { url: `/books/${book.id}` }) %>
      <a class="btn btn-primary table-details-author-button" href="/authors/<%= book.author.id %>">View Author</a>
    </div>
  </div>
  <div class="table-details-grid">
    <div class="table-details-label">Author:</div>
    <div><%= book.author.name %></div>
    <div class="table-details-label">Publish Date:</div>
    <div><%= book.publishDate.toDateString() %></div>
    <div class="table-details-label">Page Count:</div>
    <div><%= book.pageCount %></div>
    <div class="table-details-label">Description:</div>
    <div><%= book.description %></div>
  </div>
</div>